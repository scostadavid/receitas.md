---
interface Props {
  recipesCount: number;
  lastUpdate: string;
}

const { recipesCount, lastUpdate } = Astro.props;
---

<nav class="border-b-2 border-dashed border-[#262626] bg-[#0f0f0f] sticky top-0 z-50">
  <div class="max-w-6xl mx-auto px-4 py-3">
    <div class="flex items-center justify-between">
      <a href="/" class="flex items-center gap-2">
        <div class="text-2xl">🧂</div>
        <div>
          <h1 class="text-xl font-bold text-[#ededed] font-mono">Receitas Markdown</h1>
          <p class="text-xs text-[#a3a3a3]">
            Um repositório de receitas para trabalhadores remotos que cozinham 🍳
          </p>
        </div>
      </a>
    </div>

    <div class="flex items-center gap-4 text-xs text-[#a3a3a3] font-mono mt-2 pt-2 border-t border-dashed border-[#262626]/50">
      <a href="/about" class="hover:text-[#ededed] underline">
        README.md
      </a>
      <span>|</span>
      <span>{recipesCount} receitas</span>
      <span>|</span>
      <span>Última atualização: {lastUpdate}</span>
    </div>
  </div>
</nav>